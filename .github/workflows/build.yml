on:
  pull_request:
    branches:
      - 'master'
    paths:
      - '.github'
      - 'platform'
      - 'script'
      - 'shared'
      - 'tests'
  push:
    branches:
      - '**'
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
    paths-ignore:
      - 'docs/**'
      - 'examples/**'
      - 'extras/**'
      - .dockerignore
      - .gitignore
      - CHANGELOG.md
      - Dockerfile
      - LICENSE
      - README.md

jobs:
  build_macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: abdes/gha-setup-ninja@660f330a40aefa8d11632b3f901e92a4adb33d65
      - run: |
          echo "JAVA_HOME=$JAVA_HOME_11_arm64" >> $GITHUB_ENV
          echo "$JAVA_HOME_11_arm64/bin" >> $GITHUB_PATH
      # x64
      - run: python3 script/build.py --arch x64
      - run: python3 script/test.py
      - run: python3 script/package_platform.py --arch x64
      - uses: actions/upload-artifact@v4
        with:
          name: macos-x64-jars
          path: target/skija-macos-x64-*
      # arm64
      - run: python3 script/clean.py
      - run: python3 script/build.py --arch arm64
      - run: python3 script/test.py
      - run: python3 script/package_platform.py --arch arm64
      - uses: actions/upload-artifact@v4
        with:
          name: macos-arm64-jars
          path: target/skija-macos-arm64-*

  build_linux:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          echo "JAVA_HOME=$JAVA_HOME_11_X64" >> $GITHUB_ENV
          echo "$JAVA_HOME_11_X64/bin" >> $GITHUB_PATH
          sudo apt-get update
          sudo apt-get --quiet --yes install ninja-build libglu1-mesa-dev libegl-dev g\+\+-10-aarch64-linux-gnu libwayland-dev libglvnd-dev libegl1-mesa libegl1-mesa-dev
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          sudo update-alternatives --config gcc
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      # x64
      - run: python3 script/build.py
      - run: python3 script/package_shared.py
      - uses: actions/upload-artifact@v4
        with:
          name: shared-jars
          path: target/skija-shared-*.jar
      - run: python3 script/test.py
      - run: python3 script/package_platform.py
      - uses: actions/upload-artifact@v4
        with:
          name: linux-x64-jars
          path: target/skija-linux-x64-*
      # arm64
      - run: python3 script/clean.py
      - run: python3 script/build.py --arch arm64
      - run: python3 script/package_platform.py --arch arm64
      - uses: actions/upload-artifact@v4
        with:
          name: linux-arm64-jars
          path: target/skija-linux-arm64-*

  build_windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v5
      - uses: microsoft/setup-msbuild@v1
      - uses: ilammy/msvc-dev-cmd@v1
      - uses: abdes/gha-setup-ninja@660f330a40aefa8d11632b3f901e92a4adb33d65
      - shell: bash
        run: |
          echo "JAVA_HOME=$JAVA_HOME_11_X64" >> $GITHUB_ENV
          echo "$JAVA_HOME_11_X64/bin" >> $GITHUB_PATH
      - run: python3 script/build.py
      - shell: bash
        run: python3 script/test.py
      - run: python3 script/package_platform.py
      - uses: actions/upload-artifact@v4
        with:
          name: windows-x64-jars
          path: target/skija-windows-x64-*

  build_android:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - run: |
          sudo apt-get update
          sudo apt-get --yes install openjdk-11-jdk wget unzip
          wget https://dl.google.com/android/repository/commandlinetools-linux-8512546_latest.zip -O cmdline-tools.zip
          unzip cmdline-tools.zip
          sudo mkdir -p /opt/android-sdk/cmdline-tools/latest
          sudo mv cmdline-tools/* /opt/android-sdk/cmdline-tools/latest/
          sudo chown -R $USER:$USER /opt/android-sdk
          echo "y" | /opt/android-sdk/cmdline-tools/latest/bin/sdkmanager "platforms;android-33" "build-tools;33.0.0" "ndk;25.1.8937393"
          echo "ANDROID_SDK_ROOT=/opt/android-sdk" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=/opt/android-sdk/ndk/25.1.8937393" >> $GITHUB_ENV
          echo "JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64" >> $GITHUB_ENV
          echo "/opt/android-sdk/cmdline-tools/latest/bin" >> $GITHUB_PATH
      - run: python3 script/build.py --system android --arch arm64
      - run: python3 script/test.py --system android
      - run: python3 script/package_platform.py --system android --arch arm64
      - uses: actions/upload-artifact@v4
        with:
          name: android-arm64-jars
          path: target/skija-android-arm64-*

  release:
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    needs: [build_windows, build_linux, build_macos, build_android]
    steps:
      - uses: actions/checkout@v5
      - run: |
          echo "JAVA_HOME=$JAVA_HOME_11_X64" >> $GITHUB_ENV
          echo "$JAVA_HOME_11_X64/bin" >> $GITHUB_PATH
      - uses: actions/download-artifact@v4
        with:
          name: shared-jars
          path: target
      - uses: actions/download-artifact@v4
        with:
          name: macos-x64-jars
          path: target
      - uses: actions/download-artifact@v4
        with:
          name: macos-arm64-jars
          path: target
      - uses: actions/download-artifact@v4
        with:
          name: linux-x64-jars
          path: target
      - uses: actions/download-artifact@v4
        with:
          name: linux-arm64-jars
          path: target
      - uses: actions/download-artifact@v4
        with:
          name: windows-x64-jars
          path: target
      - name: Release to Maven Central
        run: |
          echo -n "$GPG_SIGNING_KEY" | base64 --decode | gpg --import
          python3 ./script/release.py
        env:
          GPG_SIGNING_KEY: ${{ secrets.GPG_SIGNING_KEY }}
          SONATYPE_BEARER_TOKEN: ${{ secrets.SONATYPE_BEARER_TOKEN }}
      - name: Delete useless jars
        run: |
          rm target/skija-macos-*-sources.jar
          rm target/skija-macos-*-javadoc.jar
          rm target/skija-linux-*-sources.jar
          rm target/skija-linux-*-javadoc.jar
          rm target/skija-windows-*-sources.jar
          rm target/skija-windows-*-javadoc.jar
      - uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_NOTES.md
          files: |
            target/*.jmod
            target/*.jar
